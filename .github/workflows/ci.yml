name: ci
on:
  push:
  pull_request:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'
      - name: Install (no code yet)
        run: echo "Nothing to install yet."
      - name: Unit tests
        run: echo "No tests yet." && exit 0
